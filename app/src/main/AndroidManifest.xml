<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.flag">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_main_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_main_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Flag">
        <activity android:name=".view.activity.addTeamActivity"></activity>
        <activity android:name=".view.activity.TeamActivity" />
        <activity android:name=".view.activity.RoomActivity" />
        <activity android:name=".view.activity.AllActivity" />
        <activity android:name=".view.activity.MainActivity" />
        <activity android:name=".view.activity.SignUpActivity" />
        <activity android:name=".view.activity.MatchActivity" />
        <activity android:name=".view.activity.SecondActivity" />
        <activity android:name=".view.activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>